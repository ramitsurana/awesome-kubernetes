sudo: required
language: ruby
rvm:
  - 2.2
before_script:
  - gem install awesome_bot
script:
  - awesome_bot README.md --allow-redirect --white-list linkedin.com,kismatic.com,slideshare.net,research.google.com,kubespray.io,twitter.com --allow-dupe
